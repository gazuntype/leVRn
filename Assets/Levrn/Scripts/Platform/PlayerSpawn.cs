using UnityEngine;
using System.Collections;

public class PlayerSpawn : MonoBehaviour {
	// Use this for initialization
	void Start () {
		transform.position = new Vector3(-2.974f, 0, 0.64f);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
